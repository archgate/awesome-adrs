# tanstack-query

Server state management with TanStack Query — caching, background refetching, and optimistic updates.

## Included ADRs

| ID     | Title                           |
| ------ | ------------------------------- |
| FE-001 | TanStack Query for Server State |

## Quick Start

Import the full pack:

```bash
archgate adr import packs/tanstack-query
```

Cherry-pick a single ADR:

```bash
archgate adr import packs/tanstack-query/adrs/FE-001-tanstack-query
```
