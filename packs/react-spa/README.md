# react-spa

Core React SPA architecture — TypeScript, Vite, component patterns, folder structure, no barrel files, no path aliases.

## Included ADRs

### Architecture

| ID       | Title                          |
| -------- | ------------------------------ |
| ARCH-001 | TypeScript as Primary Language |

### Frontend

| ID     | Title                                       |
| ------ | ------------------------------------------- |
| FE-001 | React SPA Architecture                      |
| FE-002 | Vite as Frontend Build Tool                 |
| FE-003 | Stateful and Stateless Component Separation |
| FE-004 | No Barrel Files                             |
| FE-005 | Frontend Folder Structure                   |
| FE-006 | No Path Aliases in TypeScript               |

## Quick Start

Import the full pack:

```bash
archgate adr import packs/react-spa
```

Cherry-pick a single ADR:

```bash
archgate adr import packs/react-spa/adrs/FE-001-react-spa
```
