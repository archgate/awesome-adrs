# jotai

Jotai for client-side global state — atomic state management for React applications.

## Included ADRs

| ID     | Title                   |
| ------ | ----------------------- |
| FE-001 | Jotai for Client State  |

## Quick Start

Import the full pack:

```bash
archgate adr import packs/jotai
```

Cherry-pick a single ADR:

```bash
archgate adr import packs/jotai/adrs/FE-001-jotai-client-state
```
